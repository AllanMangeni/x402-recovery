## What this changes

<!-- One paragraph describing the problem and how this PR solves it. -->

## Type of change

- [ ] Bug fix
- [ ] New feature
- [ ] Breaking change
- [ ] Documentation only
- [ ] Dependency update

## Checklist

- [ ] `npm run lint` passes (`tsc --noEmit`, zero errors)
- [ ] `npm test` passes — all tests green
- [ ] New behaviour is covered by tests
- [ ] No new hard runtime dependencies added (viem and express are the only permitted runtime deps)
- [ ] No secrets, private keys, or RPC URLs committed
- [ ] If adapter code changed: `BASE_RPC_URL` is read from environment only, not hardcoded

## Notes for reviewer

<!-- Context, tradeoffs, or anything that warrants extra attention. -->
